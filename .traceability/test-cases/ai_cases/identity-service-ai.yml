# AI-Generated Test Scenarios
# Generated: 2025-12-17T21:53:42.134Z
# (✅ = in baseline, 🆕 = new suggestion, 🔧 = modified, ⚠️ = verify)
# DO NOT EDIT - regenerated automatically
# Copy relevant 🆕 scenarios to baseline/identity-service-baseline.yml

service: identity-service

# POST /identity/register
POST /identity/register:
  happy_case:
    - When user registers with valid email, username and password, return 201  🆕
    - When user registers with all required and optional fields, return 201 with user details  🆕
    - When valid registration data is submitted, return success with authentication token  🆕
    - When new user registers with unique credentials, create account and return 201  🆕

  edge_case:
    - When email contains special characters (e.g., user+tag@domain.com), accept and return 201  🆕
    - When username is at minimum length boundary, accept and return 201  🆕
    - When username is at maximum length boundary, accept and return 201  🆕
    - When password contains unicode characters, accept and return 201  🆕
    - When request body is empty, return 400  🆕
    - When optional fields are null or missing, accept and return 201  🆕
    - When email has maximum allowed length, accept and return 201  🆕
    - When username contains allowed special characters, accept and return 201  🆕

  error_case:
    - When required fields are missing, return 400  🆕
    - When email format is invalid, return 400 or 422  🆕
    - When password does not meet complexity requirements, return 400 or 422  🆕
    - When email already exists, return 409  🆕
    - When username already exists, return 409  🆕
    - When request body has invalid JSON format, return 400  🆕
    - When email exceeds maximum length, return 400 or 422  🆕
    - When username contains invalid characters, return 400 or 422  🆕
    - When password is too short, return 400 or 422  🆕
    - When Content-Type header is missing or incorrect, return 415  🆕
    - When server encounters internal error during registration, return 500  🆕

  security:
    - When email contains SQL injection payload, reject with 400  🆕
    - When username contains SQL injection payload, reject with 400  🆕
    - When password contains SQL injection payload, sanitize and accept or reject with 400  🆕
    - When email contains XSS script tags, reject with 400  🆕
    - When username contains XSS script tags, reject with 400  🆕
    - When request includes script injection in any field, reject with 400  🆕
    - When attempting registration without proper CSRF token, return 403  🆕
    - When password is sent in plain text over non-HTTPS, reject connection  🆕
    - When attempting to register with admin/privileged role in payload, reject with 403  🆕
    - When request contains path traversal attempts in fields, reject with 400  🆕
    - When excessive registration attempts from same IP, return 429 rate limit error  🆕

# POST /identity/login
POST /identity/login:
  happy_case:
    - When user logs in with valid username and password, return 200 with auth token  ✅
    - When user logs in with valid email and password, return 200 with auth token  ✅
    - When user logs in with correct credentials, return user profile data  ✅
    - When user logs in successfully, return refresh token along with access token  ✅

  edge_case:
    - When username contains special characters, validate and process login  🆕
    - When password is at minimum length boundary, accept if valid  🆕
    - When password is at maximum length boundary, accept if valid  🆕
    - When username has leading/trailing whitespace, trim and process  🆕
    - When request body is empty, return 400 with appropriate error  🆕
    - When username field is null, return 400 with validation error  🆕
    - When password field is null, return 400 with validation error  ✅
    - When username contains unicode characters, validate and process  🆕
    - When multiple concurrent login requests for same user, handle gracefully  🆕

  error_case:
    - When username does not exist, return 401 unauthorized  ✅
    - When password is incorrect, return 401 unauthorized  ✅
    - When required username field is missing, return 400 bad request  🆕
    - When required password field is missing, return 400 bad request  ✅
    - When user account is locked/disabled, return 403 forbidden  ✅
    - When user account is not verified, return 403 forbidden  ✅
    - When invalid JSON format in request body, return 400 bad request  🆕
    - When content-type header is missing or incorrect, return 400 bad request  ✅
    - When too many failed login attempts, return 429 rate limit exceeded  ✅
    - When database connection fails, return 500 internal server error  🆕
    - When authentication service is unavailable, return 503 service unavailable  🆕

  security:
    - When username contains SQL injection payload, sanitize and reject with 400  🆕
    - When password contains SQL injection payload, sanitize and reject with 400  🆕
    - When username contains XSS script tags, sanitize and reject with 400  🆕
    - When password contains XSS payload, sanitize and reject with 400  🆕
    - When attempting login without CSRF token, reject with 403  🆕
    - When using expired or invalid session token, return 401  🆕
    - When attempting brute force with multiple failed attempts, lock account and return 429  ✅
    - When credentials are sent over non-HTTPS connection, reject request  🆕
    - When attempting authentication bypass with manipulated headers, return 401  🆕
    - When using default or common passwords, enforce password policy and reject  🆕

# POST /identity/verify-otp
POST /identity/verify-otp:
  happy_case:
    - When valid OTP and identifier provided, return 200 and verify successfully  ✅
    - When correct OTP submitted within expiry time, return success with auth token  ✅
    - When valid 6-digit OTP matches sent code, return 200 with verified status  🆕
    - When OTP verified for registered user, return 200 with user session  🆕

  edge_case:
    - When OTP is empty string, return 400 validation error  ✅
    - When OTP is null, return 400 bad request  ✅
    - When OTP has leading/trailing spaces, trim and validate  🆕
    - When OTP is exactly at expiry timestamp, return 400 expired  ✅
    - When identifier is missing, return 400 required field error  🆕
    - When OTP contains special characters, return 400 invalid format  🆕
    - When OTP is alphabetic instead of numeric, return 400 invalid format  ✅
    - When request body is empty object, return 400 missing required fields  🆕
    - When OTP length is less than required digits, return 400 invalid length  ✅
    - When OTP length exceeds maximum digits, return 400 invalid length  🆕

  error_case:
    - When OTP is incorrect, return 400 invalid OTP error  ✅
    - When OTP has expired, return 400 OTP expired error  ✅
    - When OTP already used, return 400 OTP already verified error  ✅
    - When identifier not found, return 404 user not found  🆕
    - When maximum retry attempts exceeded, return 429 too many requests  🆕
    - When no OTP was sent to identifier, return 404 OTP not found  ✅
    - When authentication token missing, return 401 unauthorized  ✅
    - When authentication token invalid, return 401 invalid token  ✅
    - When user account is locked, return 403 account locked  🆕
    - When request body malformed JSON, return 400 bad request  🆕
    - When database connection fails, return 500 internal server error  🆕
    - When OTP service unavailable, return 503 service unavailable  🆕

  security:
    - When OTP contains SQL injection payload, sanitize and return 400  🆕
    - When identifier contains SQL injection, reject with 400  🆕
    - When OTP contains XSS script tags, sanitize and return 400  🆕
    - When brute force multiple wrong OTPs, lock account after threshold  🆕
    - When request from different IP than OTP generation, flag suspicious activity  🆕
    - When concurrent verification requests for same OTP, allow only first  🆕
    - When attempting to verify without rate limiting, enforce rate limits  🆕
    - When replaying previously valid OTP, return 400 already used  🆕
    - When CSRF token missing or invalid, return 403 forbidden  🆕
    - When request lacks proper content-type header, return 415 unsupported media type  🆕

