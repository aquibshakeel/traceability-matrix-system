{
  "require": ["ts-node/register"],
  "extensions": ["ts"],
  "spec": ["tests/**/*.spec.ts"],
  "timeout": 30000,
  "color": true,
  "reporter": "spec",
  "slow": 5000,
  "bail": false,
  "recursive": true,
  "exit": true
}
